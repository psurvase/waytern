import InfoComponent from "./InfoComponent";import {Box, Button, Card, Grid, IconButton, InputBase} from "@mui/material";import SearchIcon from "@mui/icons-material/Search";import MenuIcon from "@mui/icons-material/Menu";import Link from "next/link";import AddIcon from "@mui/icons-material/Add";import React, {useEffect} from "react";import {useAppContext} from "../../context/AppContext";import {makeStyles} from "@mui/styles";const useStyles = makeStyles({    gridStyle: {        padding: "1rem",        display: 'flex',        justifyContent: 'space-between'    },    addButtonStyle:{        textTransform:"capitalize"    }});const ListComponent = ({devices})=>{    const classes = useStyles();    const {setPageName} = useAppContext();    useEffect(() => {        setPageName("Devices Form");    }, []);    return (        <>                <Grid                    component={Card}                    container                    alignItems="center"                    className={classes.gridStyle}                >                    <Grid item md={8} xs={12} sm={12}>                        <IconButton type="submit" aria-label="search">                            <SearchIcon/>                        </IconButton>                        <InputBase                            placeholder="Search"                            inputProps={{"aria-label": "search"}}                        />                        <IconButton type="submit" aria-label="search">                            <MenuIcon/>                        </IconButton>                        <InputBase                            placeholder="Advance Search"                            inputProps={{"aria-label": "search"}}                        />                    </Grid>                    <Grid item xs={2} md={2} sm={2}></Grid>                    <Grid item xs={12} md={2} sm={2} textAlign="right">                        <Link href={"/device/create/"}>                            <Button variant="contained" className={classes.addButtonStyle} endIcon={ <AddIcon/>}>                                Add device                            </Button>                        </Link>                    </Grid>                </Grid>                <Grid container mt={1}>                    {devices && devices.map(                        (device, index) => (                            <Grid item xs={12} key={index}>                                <InfoComponent device={device}></InfoComponent>                            </Grid>                        )                    )}                </Grid>        </>    )}export default ListComponent;